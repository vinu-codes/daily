# initialising packages

- npm init command (entered in default information about project, pressed enter for most of the options)
- npm install (needed to install my dev dependencies & dependencies `devDependencies`, `dependencies`)
